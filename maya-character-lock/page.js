const source='../prompts/maya-character-lock.json';const code=document.querySelector('#json');fetch(source).then(response=>response.json()).then(data=>{code.textContent=JSON.stringify(data,null,2)}).catch(()=>{code.textContent='The JSON source could not be loaded.'});document.querySelector('#copy').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(code.textContent);document.querySelector('#status').textContent='JSON copied to clipboard.'}catch{document.querySelector('#status').textContent='Select the JSON above to copy it.'}});
